export function DiagnosticsPage() {
  return (
    <div className="review-view card">
      <h2 className="section-title">Diagnostics</h2>
      <p className="muted">
        Placeholder for internal diagnostics (health checks, worker status, etc.). Add routes and tools
        here as the admin app grows.
      </p>
    </div>
  );
}
